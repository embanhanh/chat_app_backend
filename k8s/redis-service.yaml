apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: redis-cluster
  ports:
  - port: 6379
    name: client
  - port: 16379
    name: cluster